#pragma once

class neural_network {
    private:
        int inputs;
        vector<int> hidden_layers;
        int outputs;
    public:
        neural_network(int inputs, vector<int> hidden_layers, int outputs) {}
        ~neural_network() {}
 
        feed_forward(vector<double> input) {}

}